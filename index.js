/**
 * @format
 */

import {AppRegistry} from 'react-native';
import FullApp from './indexDev.tsx';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => FullApp);
